spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

app:
  jwt:
    secret: testSecretKeyForJWTTesting2024VeryLongKeyForTesting
    expiration: 86400000

razorpay:
  key:
    id: test_key_id
    secret: test_key_secret

file:
  upload:
    dir: /tmp/fashionstore-test-uploads/
